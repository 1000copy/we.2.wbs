<view>
  <!-- <text>{{title}}</text> -->
  <view class="icons">
      <image src="../../image/{{icon}}.png" mode="aspectFit"/>
      <text class="remark">{{remark}}</text>
  </view>
  <view class="list">
    <text wx:for="{{list}}">
      {{item}}
    </text>
  </view>
</view>